"use client";

import React from 'react';
import { TelemetryStreamView } from '../components/telemetry/TelemetryStreamView';

/**
 * ============================================================================
 * 📡 TelemetryPage (Страница прямого эфира телеметрии, карты и инцидентов)
 * ============================================================================
 * 
 * 📌 ЗА ЧТО ОТВЕЧАЕТ:
 * - Мониторинг прямого потока пакетов телеметрии: POST /api/v1/telemetry/ingest.
 * - Интерактивная векторная карта Сеула (район Каннам):
 *     • Анимированное движение автомобилей по контрольным точкам (Gangnam Station, COEX, Sinsa и др.)
 *     • Границы геозоны (Geofence Bounds) с визуальной индикацией выхода за пределы
 *     • Радарные маркеры статусов: Зеленый (Норма), Желтый (Сбой сенсоров), Красный (Спуфинг/Оффлайн)
 *     • Всплывающие подсказки при наведении на маркер авто
 * - JSON Payload Terminal: просмотр сырого JSON-пакета последнего тика в реальном времени с кнопкой Copy.
 * - Incident Stream Table: таблица зафиксированных инцидентов и событий Heartbeat Timeout (INC_HEARTBEAT_LOST).
 * - Фильтрация потока данных по конкретному автомобилю (Node Filter).
 * 
 * 🧩 ИСПОЛЬЗУЕМЫЕ КОМПОНЕНТЫ:
 * - TelemetryStreamView: контейнер экрана
 *   ├── GangnamVectorMap: SVG-карта Каннама с трекингом авто и геозоной
 *   ├── JsonPayloadViewer: консоль с подсветкой и копированием JSON-пакета
 *   └── IncidentsTable: журнал инцидентов и алертов безопасности
 */
export const TelemetryPage: React.FC = () => {
  return <TelemetryStreamView />;
};
